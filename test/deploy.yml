- hosts: all
  tasks:
  - name: Déploiement site web
    git:
      repo: "{{repo}}"
      dest: /var/www/html/"{{git_dest}}"
    
  - name: log deploiement site web
    debug:
      msg:
        - "source git : {{repo}}"
        - "dossier dest : /var/www/html/{{git_dest}}"
